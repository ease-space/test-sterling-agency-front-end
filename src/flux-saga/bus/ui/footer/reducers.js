export const initialState = {
  buyers: 5000000,
  suppliers: 1640,
  products: 21416,
};

export const footerReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
